{% extends 'Frontend/Templates/frontend_layout.html.twig' %}

{% block frontend_content %}
    <section>

        <p>Voici l'ensemble des photos disponibles à la vente</p>

        <div class="affichage_available_photos">
            {% for photo in all_available_photos %}
                <div class="available_photo">
                    {{ photo.serial_number }} : {{ photo.nom }}
                    <img alt="description" src="/images/{{ galeries[photo.galerie_id()].nom_galerie ~ '/' ~ photo.serial_number }}">
                </div>
            {% endfor %}
        </div>
    </section>
{% endblock %}